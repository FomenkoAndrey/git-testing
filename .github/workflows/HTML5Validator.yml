name: HTML Check
on:
  pull_request:
  push:
    branches:
      - master
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checks HTML5
      - uses: Cyb3r-Jak3/html5validator-action@<latest version>
        with:
          root: /
